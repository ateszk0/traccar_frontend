# Project Rules

- **Uniform Versioning**: Always ensure the version query parameter (e.g., `?v=12`) is uniform across all imports in all javascript files (`.js`) and link/script tags in `index.html`.
